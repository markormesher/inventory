extends ../_shared/page-layout

block page-body

	div.x_panel
		div.x_title
			h2 Dashboard
			div.clearfix

		div.x_content

			p Hi

			- debug = {user: user, allPermissions: PERMISSIONS}
			pre= JSON.stringify(debug, null, 2)
